/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "dewey.tailorbrands.com",
            "raw.githubusercontent.com"
        ]
    }
}

module.exports = nextConfig
